'use client';

// Cleaned up the relative pathing to utilize your working absolute root alias configuration 
import VaultDashboard from '@/components/VaultDashboard';

export default function Home() {
  return <VaultDashboard />;
}